name: Docker Build & Push

on:
  push:
    branches: [ master, main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ master, main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  docker:
    name: Build and Push phlex-server
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          - dockerfile: docker/Dockerfile
            tag: latest
            platform: linux/amd64,linux/arm64
          - dockerfile: docker/Dockerfile.nvidia
            tag: nvidia
            platform: linux/amd64
          - dockerfile: docker/Dockerfile.intel
            tag: intel
            platform: linux/amd64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: ${{ matrix.platform }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ matrix.tag }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            PHP_VERSION=8.3
            PHLEX_VERSION=${{ github.ref_name }}

  # TODO: hub Dockerfile lives in detain/phlex-hub; ensure the workflow picks it up
  # via the hub repo's release event. For now, this job clones the hub repo and
  # builds from its Dockerfile so the structure exists. If detain/phlex-hub does
  # not yet contain a Dockerfile, this job will fail loudly — that is intentional
  # and the next PR (in the phlex-hub repo) should add it.
  docker-hub:
    name: Build and Push phlex-hub
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout phlex-hub repository
        uses: actions/checkout@v4
        with:
          repository: detain/phlex-hub
          ref: master
          path: phlex-hub

      - name: Verify hub Dockerfile exists
        run: |
          if [ ! -f phlex-hub/Dockerfile ]; then
            echo "::error::phlex-hub/Dockerfile not found in detain/phlex-hub@master."
            echo "Add a Dockerfile to the phlex-hub repo before this workflow can succeed."
            exit 1
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute hub image tags
        id: hubmeta
        run: |
          SHA="${GITHUB_SHA::12}"
          echo "tags=${{ env.REGISTRY }}/detain/phlex-hub:latest,${{ env.REGISTRY }}/detain/phlex-hub:${SHA}" >> "$GITHUB_OUTPUT"

      - name: Build and push phlex-hub
        uses: docker/build-push-action@v5
        with:
          context: phlex-hub
          file: phlex-hub/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.hubmeta.outputs.tags }}
          cache-from: type=gha,scope=phlex-hub
          cache-to: type=gha,mode=max,scope=phlex-hub

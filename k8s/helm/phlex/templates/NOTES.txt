Phlix Media Server has been installed.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Chart: {{ .Chart.Name }}-{{ .Chart.Version }} (app {{ .Chart.AppVersion }})

{{- if .Values.ingress.enabled }}

Access the application at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}

To access the application:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "phlix.fullname" . }} 8080:{{ .Values.service.http.port }}
  Then open http://localhost:8080

{{- end }}

Useful commands:
  helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} -f

{{- if .Values.hub.enabled }}

Hub integration is ENABLED — this server will register with: {{ default .Values.config.hub_url .Values.hub.url }}
{{- end }}
{{- if .Values.hwAccel.enabled }}

Hardware acceleration is ENABLED (runtimeClassName={{ .Values.hwAccel.runtimeClassName | default "<unset>" }}).
Ensure the cluster has the matching runtime / device plugin installed.
{{- end }}
